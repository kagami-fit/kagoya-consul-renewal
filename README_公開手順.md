# KAGOYAサイト 公開・更新ガイド

このフォルダには、株式会社籠や様のリニューアルサイト一式が入っています。現在はGitHub Pagesで公開しており、HTMLや画像を更新してGitHubへ反映すると公開サイトも更新されます。

## 現在の公開先

- 公開サイト：https://kagami-fit.github.io/kagoya-consul-renewal/
- GitHub：https://github.com/kagami-fit/kagoya-consul-renewal

## いちばん簡単な公開方法

### GitHub Pages（現在利用中）

1. Codexへ「この箇所を直してGitHubへ反映して」と日本語で伝えます。
2. 修正後に表示確認を行います。
3. 問題がなければGitHubへ保存します。
4. 通常は数分以内に公開サイトへ反映されます。

### Netlify Dropで別の確認URLを作る場合

1. `zoroya-site.zip` を解凍します。
2. [Netlify Drop](https://app.netlify.com/drop) を開きます。
3. 解凍したフォルダを画面へドラッグします。
4. 発行されたURLでスマホとパソコンの表示を確認します。

## 写真を差し替える方法

画像は `src/` にまとまっています。同じファイル名で上書きすると、HTMLを貼り直さずに差し替えられます。

- ヒーロー写真：`src/wp-hero.jpg`／`src/wp-hero.webp`
- ロゴ：`src/logo.png`
- 相談シーン：`src/wp-consultation.jpg`／`src/wp-consultation.webp`
- サービス写真：`src/wp-sell.*`、`src/wp-buy.*`、`src/wp-manage.*`、`src/wp-invest.*`、`src/wp-finance.*`、`src/wp-inherit.*`、`src/wp-income.*`、`src/wp-renovation.*`
- 物件・事例写真：`src/wp-property-*`、`src/wp-case-map.*`
- 画像の取得元一覧：`src/wp-image-sources.md`
- OGP・SNS共有画像：`src/ogp.jpg`

PDFから抽出した画像は使用しません。新しい画像を追加する場合も、既存WordPressの「メディア」からファイルURLをコピーし、取得元を `src/wp-image-sources.md` へ追記します。元画像の比率に近い写真を使うと崩れにくくなります。差し替え後は、画像最適化と375px・768px・1280pxでの表示確認を行ってください。

## お問い合わせ導線を変更する方法

静的サイトのフォーム案内は `contact.html` にあります。Googleフォーム、Tally、Formspreeを使う場合は、サービス側でフォームを作成し、そのURLへボタンのリンク先を変更します。

- Googleフォーム：社内で使い慣れている場合
- Tally：見た目を整えたい場合
- Formspree：現在のデザインを保ったまま送信機能だけ追加したい場合

送信先メールアドレスやパスワードはHTMLへ書かず、各サービスの安全な設定画面で管理してください。

## WordPressへ移すタイミング

次のどれかが必要になったらWordPress移行を検討します。

- 自社で物件情報を頻繁に更新したい
- ブログやお知らせを管理画面から投稿したい
- 問い合わせ内容を管理画面で確認したい
- 担当者ごとに更新権限を分けたい

移行にはWordPressのURL、管理ユーザー、アプリケーションパスワードが必要です。これらは `.env` ファイルだけに保存し、チャットやHTMLへ貼り付けないでください。

## 公開後の更新ガイド

### WordPressでの日常更新（移行後に自分でできる範囲）

WordPress未移行の現段階では管理画面の実スクリーンショットはありません。移行時に実際の管理画面を撮影し、この章へ追加します。

1. ブログ・お知らせ：管理画面の「投稿」→「新規投稿を追加」→本文と画像を入力→「下書き保存」→内容確認→「公開」。
2. 固定ページ：管理画面の「固定ページ」→直したいページ→文言を修正→「更新」。
3. メニュー：「外観」→「メニュー」→項目をドラッグして並べ替え→「メニューを保存」。
4. 画像：「メディア」→「新しいメディアファイルを追加」→画像を選択→対象ページで差し替え→「更新」。
5. 公開確認：サイトを別タブで開き、スマートフォン表示まで確認します。

### AIに頼む更新（スクリーンショット方式・推奨）

直したい箇所をスクリーンショットで撮り、その画像をCodexへ貼って日本語で依頼してください。

依頼例：

- 「ファーストビューの色をもう少し濃い紺にして」
- 「料金表に3つ目のプランを追加して」
- 「この写真を添付した写真へ差し替えて」
- 「スマホでこの見出しが読みにくいので直して」
- 「物件情報を1件追加してGitHubへ反映して」

修正後は、公開前にスクリーンショットとURLの両方で確認します。

### 依頼できる更新と別途設計が必要なもの

通常の更新で対応しやすいもの：

- 文言、色、画像の差し替え
- お知らせ、ブログ、物件の追加
- FAQや小さなセクションの追加
- 新しい固定ページの追加

別途見積り・設計が必要になりやすいもの：

- サイト全体の大幅リニューアル
- 会員機能、予約、決済などの新機能
- 大量の物件データ移行
- 本格的なSEO、AIO、GEO設計と継続運用

## 更新時の鉄則

- GitHubの履歴を残してから公開し、前の状態へ戻せるようにします。
- 画像は同じファイル名で差し替えると貼り直しが不要です。
- 公開前に375px・768px・1280pxで、横はみ出し、重なり、不自然な改行を確認します。
- ブラウザのエラーと画像・リンクの404がないことを確認します。
- 会社情報、物件価格、営業時間、法的な文章は事業者側で最終確認します。

## SEO・AI検索について

この静的サイトには、ページごとのタイトル・説明文、OGP、構造化データ、FAQ、サイトマップを設定しています。ただし、本格的なSEO・AIO・GEOは、公開後の記事更新、一次情報の追加、検索状況の分析、内容改善を継続して行う必要があります。
